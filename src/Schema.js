// Takes a list of top-level services and produces
// a GraphQL Schema

// SchemaBuilder :: [ Service ] -> GraphQL Schema
function Schema (services) {

  const schema = {}

  return schema

}

export default Schema
